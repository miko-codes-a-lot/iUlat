package dev.cloudants.iulat.lib.models.domain

import dev.cloudants.iulat.lib.models.entities.UserDto

// only a placeholder, we might not use this
data class LoginResponseDto(
    private val user: UserDto,
)
